package square1;

import java.awt.Color;

public interface Constants  {
  Color orange=new Color(0xff8f00);
  double pi=Math.acos(-1.0);
  double deg2rad=Math.PI/180.;
  double sin15=Math.sin(15.*deg2rad);
  double cos15=Math.cos(15.*deg2rad);
  double sin30=Math.sin(30.*deg2rad);
  double cos30=Math.cos(30.*deg2rad);
  double tan45=Math.tan(45.*deg2rad);
  double cos45=Math.cos(45.*deg2rad);
}
